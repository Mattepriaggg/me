// menu
let hamburger = document.getElementById('hamburger_menu');

let navUl = hamburger.firstElementChild;

// quando abbassi la visuale della finestra lo scroll va avanti
window.addEventListener('scroll', () => {
    
})